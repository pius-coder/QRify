# Step 02: Plan

**Task:** F00 initialize Hono backend
**Started:** 2026-06-24T13:33:24Z

---

## Planning Progress

_Implementation plan will be written here..._

## Implementation Plan: F00 initialize Hono backend

### Overview
Initialize a Hono backend project with Bun runtime, TypeScript, and essential middleware (CORS, error handling) following the PROJECT.md specifications.

### Prerequisites
- [ ] Bun installed (version 1.1+)
- [ ] Node.js not required (Bun handles everything)

---

### File Changes

#### Project Initialization (Bash)
- Run `bun create hono@latest qrify-backend --template bun`
- This creates the base project structure
- Generated files: src/index.ts, package.json, tsconfig.json, .gitignore

#### `package.json`
- Verify generated scripts include `"dev": "bun run --hot src/index.ts"`
- Add additional scripts if needed:
  - `"start": "bun run src/index.ts"`
  - `"build": "bun build src/index.ts --outdir ./dist --target bun"`
  - `"test": "bun test"`
  - `"typecheck": "tsc --noEmit"`

#### `tsconfig.json`
- Verify strict mode is enabled
- Verify JSX configuration for Hono JSX support
- Add any additional compiler options if needed

#### `src/config/env.ts` (NEW FILE)
- Create environment variable validation with Zod
- Define schema for:
  - `PORT` (default: 3000)
  - `CORS_ORIGIN` (default: http://localhost:5173)
  - `NODE_ENV` (default: development)
- Export validated environment object

#### `src/index.ts`
- Import and configure CORS middleware from `hono/cors`
- Import environment config from `./config/env`
- Add health check route: `GET /api/v1/health`
- Add global error handler with `app.onError()`
- Add 404 handler with `app.notFound()`
- Configure server with custom port from env
- Export server configuration for Bun

#### `src/utils/errors.ts` (NEW FILE)
- Create custom error classes following PROJECT.md error format
- Implement `ApiError` class with status, code, message, fields
- Export error response formatter

#### `.env.example` (NEW FILE)
- Create example environment file with all required variables
- Document each variable with comments

---

### Testing Strategy

**New tests:**
- `src/index.test.ts` - Test health check endpoint returns 200
- `src/config/env.test.ts` - Test environment validation

**Update existing:**
- None (greenfield project)

---

### Acceptance Criteria Mapping
- [x] AC1: Satisfied by project initialization with Hono CLI
- [x] AC2: Satisfied by TypeScript configuration in tsconfig.json
- [x] AC3: Satisfied by `bun run --hot` script in package.json
- [x] AC4: Satisfied by health check route in src/index.ts
- [x] AC5: Satisfied by src/config/env.ts with Zod validation
- [x] AC6: Satisfied by CORS middleware in src/index.ts
- [x] AC7: Satisfied by app.onError() handler in src/index.ts
- [x] AC8: Satisfied by following PROJECT.md structure
- [x] AC9: Satisfied by not installing any ORM
- [x] AC10: Satisfied by package.json generated by CLI

---

### Risks & Considerations
- Risk 1: Hono CLI may prompt for template selection → Use `--template bun` flag
- Risk 2: Port 3000 may be in use → Configure via environment variable
- Risk 3: CORS origin may differ in production → Use environment-based configuration

---

## Step Complete
**Status:** ✓ Complete
**Files planned:** 6 files
**Tests planned:** 2 test files
**Next:** step-03-execute.md
**Timestamp:** 2026-06-24T13:35:00Z
